# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'pPoint' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for pPoint
  pod 'Firebase/Core'
  pod 'Firebase/AdMob'
  pod 'Firebase/Auth'

  target 'pPointTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'pPointUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
